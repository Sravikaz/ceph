overrides:
  ceph:
    log-whitelist:
      - overall HEALTH_
      - \(CACHE_POOL_NO_HIT_SET\)
      - \(POOL_APP_NOT_ENABLED\)
      - 'Failed to load ceph-mgr modules: rook'
tasks:
- workunit:
    clients:
      client.0:
        - rbd/test_librbd.sh
    env:
      RBD_FEATURES: "125"
